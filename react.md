# React Cheatsheet (including Next etc.)

## New in React

1. `use` hook is in a sense a React Query killer -- it combines `useEffect` and `useState` in a certain manner, useful as a data fetching shorthand.
