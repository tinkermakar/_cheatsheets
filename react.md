# React Cheatsheet (including Next etc.)

## New in React

1. `use` hook is in a sense a "React Query" for server-side components -- it combines `useEffect` and `useState` in a certain manner, useful as a data fetching shorthand.

## Performance

1. Use, `data-*` attributes instead of inc-callback variables where possible ( credit: https://dev.to/moruno21/99-of-frontend-devs-dont-use-this-1g44)
